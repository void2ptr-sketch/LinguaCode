export type Environment = {
  production: boolean;
  apiUrl: string;
  fixturesUrl: string;
  appName: string;
  useCardsApiMock: boolean;
  useScenariosApiMock: boolean;
  useCoursesApiMock: boolean;
};
